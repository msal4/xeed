@model ExceedConsultancy.Models.LoginViewModel;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers                                                                                                  @using Microsoft.AspNetCore.Localization;

@using Microsoft.AspNetCore.Localization;

@{
    var culture = ViewContext.HttpContext.Features.Get<IRequestCultureFeature>().RequestCulture.Culture.Name;
    var textDirection = culture.StartsWith("ar") ? "rtl" : "ltr";
}
@{
    Layout = "~/Views/Shared/_Admin.cshtml";
}



<section id="CreatePage" class="FormHeight" >
    <div class="container formCRUD">

        <div class=" text-center">
            <h1>LOGIN</h1>
        </div>

        <div class="formContainer bglogin">
            @if (TempData["fail"] != null)
            {
                <p class="alert alert-danger d-block">@TempData["fail"]</p>
            }

            <form method="post" id="form-id" enctype="multipart/form-data">
                <div class="row">

                    <div class="col-12 col-md-12 align-items-start">
                        <div class="form-group">
                            <label for="name">UserName*</label>
                            <input class="form-control form-control-lg" type="text" id="UserName" name="UserName" />
                        </div>
                    </div>

                    <div class="col-12 col-md-12 align-items-start">
                        <div class="form-group">
                            <label for="name">Password*</label>
                            <input class="form-control form-control-lg" type="password" id="Password" name="Password" />
                        </div>
                    </div>
                </div>
                
                <div class="form-group text-center">
                    <button type="submit" class="btn btn-primary p-4 f-4 h5 w-auto">Submit</button>
                </div>
            </form>
        </div>
    </div>
</section>


